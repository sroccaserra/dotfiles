# Rake FTW

dir = '~/dotfiles'
olddir = '~/.dotfiles_backup'

files_to_symlink = ['bash_aliases', 'emacs', 'inputrc', 'noserc', 'tmux.conf',
                    'vimrc']
files_to_source = ['bash_profile', 'bashrc']
required_commands = ['curl', 'git', 'python', 'ruby', 'wget']

task :default do |t|
    p 'hello Rake'
end
